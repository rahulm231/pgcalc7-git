/*
jQuery(document).ready(function($){
    var resize_slider = function(){
        $('.meerkat-slider-inner').each(function(){
            var img_height = $(this).find('img')[0].height;
            $(this).parents('.slider-wrapper').height(img_height);
        });
    };
    $(window).resize(resize_slider);
    setTimeout(resize_slider, 1000);
});*/
